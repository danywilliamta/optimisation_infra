[project]
name = "optimisation-infra"
version = "0.1.0"
description = "Add your description here"

authors = [
    { name = "Dany Tagne", email = "dany-william.tagne@inovaxo.fr" }
]
requires-python = ">=3.12"
dependencies = [
    "dotenv>=0.9.9",
    "ijson>=3.4.0",
    "openai>=2.1.0",
    "psycopg2>=2.9.10",
    "requests>=2.32.5",
]
[tool.hatch.build.targets.wheel]
packages = ["optimisation_infra"]

[build-system]
requires = ["hatchling"]
build-backend = "hatchling.build"
